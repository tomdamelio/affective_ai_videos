# Scripts archivados (no son parte del flujo actual)

Diagnósticos y one-shots del piloto E01 (jun 2026). Se conservan como registro.
Para generar estímulos, usar los scripts del flujo (ver ../../docs/PLAYBOOK_GENERACION.md):
new_stimulus, pilot_v1, derive_variant, start_variants, align_start_look,
finalize_frames, extract_frames, run_videos (+ stimulus.py).

- control_test_jan_method / test_flux_dev_* / test_imminent_threat / test_safety_filter:
  pruebas de moderación y de modelos (cómo descubrimos qué modelo usar).
- pilot_epss_keyframes: primer intento de pipeline (superado por el flujo actual).
- migrate_to_dataset: migración one-shot de par03 -> E01 (ya ejecutada).
